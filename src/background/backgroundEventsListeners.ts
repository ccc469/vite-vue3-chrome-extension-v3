class BackgroundEventsListeners {
  static async onCommand(command: string) {
    console.log(
      '🚀 ~ file: backgroundEventsListeners.ts:3 ~ BackgroundEventsListeners ~ onCommand ~ command:',
      command
    )
  }
}
export default BackgroundEventsListeners
